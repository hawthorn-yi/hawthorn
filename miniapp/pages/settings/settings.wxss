.settings-section { padding: 24rpx; }
.section-title { display: block; font-size: var(--font-lg); margin-bottom: 16rpx; color: var(--color-text-secondary); }
.setting-item { padding: 24rpx; }
.setting-icon { margin-right: 12rpx; font-size: 32rpx; }
